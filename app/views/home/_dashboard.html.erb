<nav id="home-nav" class="navbar navbar-expand-lg navbar-light fixed-top">
  <form class="form-inline float-right">
    <div class="form-control mr-sm-2">Date: <%= Date.today.strftime("%B %d, %Y") %></div>
    <div class="form-control mr-sm-2">Time: <%= Time.now.strftime("%I:%M %p") %></div>
  </form>
</nav>
<div class="wrapper d-flex">
  <div class="sidebar">
    <div class="current-user">
        Hello, <%= current_user.first_name.capitalize %>
    </div>
      <ul class="nav flex-column" >
        <li class="nav-item">
            <a>
            <i class="fas fa-folder-open"></i>
            <%= link_to 'Show Categories', categories_path %>
            </a>
        </li>
        <li class="nav-item">
            <a>
            <i class="fas fa-folder-plus"></i>
            <%= link_to 'New Category', new_category_path %>
            </a>
        </li>
        <li class="nav-item">
            <a>
            <i class="fas fa-id-badge"></i>
            <%= link_to 'Update Profile', edit_user_registration_path %>
            </a>
        </li>
        <li class="nav-item">
            <a>
            <i class="fas fa-sign-out-alt"></i>
            <% if user_signed_in? %>
            <%= link_to 'Sign Out', destroy_user_session_path, method: :delete %>
            <% end %>
            </a>
        </li>
      </ul>
  </div>
</div>
